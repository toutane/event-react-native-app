import React from "react";
import FirebaseInitialization from "./app/firebase/firebaseInitialization";

export default class App extends React.Component {
  render() {
    return <FirebaseInitialization />;
  }
}
